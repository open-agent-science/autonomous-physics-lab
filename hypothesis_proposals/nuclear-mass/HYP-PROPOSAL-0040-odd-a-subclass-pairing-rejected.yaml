id: HYP-PROPOSAL-0040
title: "Separate odd-A sub-class pairing (even-Z/odd-N vs odd-Z/even-N)"
campaign_profile_id: nuclear-mass-surface
proposal_kind: formula_search
status: REJECTED

claim_ceiling: "Rejected before execution; no sandbox evidence exists."

proposed_by:
  contributor_id: oleg
  agent_id: claude
  task_id: TASK-0201

input_references:
  - "campaign_profiles/nuclear-mass-surface.yaml"
  - "data/nuclear_masses/nmd-0002-curated-measured-slice.yaml"

hypothesis:
  statement: >
    Splitting the odd-A class into even-Z/odd-N and odd-Z/even-N subclasses
    and adding independent constant corrections for each might capture
    proton-neutron pairing asymmetry.
  variables:
    - "eta_eZoN = int(Z%2==0 and N%2==1)"
    - "eta_oZeN = int(Z%2==1 and N%2==0)"
    - "c_eZoN, c_oZeN: train-fitted coefficients"
  assumptions:
    - "Both odd-A sub-classes have sufficient training representatives."
  expected_scope: "NMD-0002 structured holdout."
  expected_failure_modes:
    - "NMD-0002 has zero odd-Z/even-N nuclei; c_oZeN is completely unidentifiable."

novelty_check:
  checked_files:
    - "hypothesis_proposals/nuclear-mass/HYP-PROPOSAL-0038-differential-pairing-ee-oo.yaml"
  possible_overlap: "Extends the pairing class indicator approach of HYP-PROPOSAL-0038."
  notes: "Rejected before execution due to data-sparsity overfit."

falsification_plan:
  deterministic_check: "Not executed; rejected before execution."
  baseline_or_reference: "RESULT-0015 frozen baseline."
  pass_condition: "Not applicable; rejected."
  fail_condition: "Not applicable; rejected."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not claim any result from this unexecuted proposal."
  mitigations:
    - "Rejected before execution."

sandbox_evidence:
  commands:
    - "N/A: rejected before execution"
  code_references:
    - "scripts/run_nuclear_pairing_batch.py"
  metrics:
    - "N/A: rejected before execution"
  output_paths:
    - "agent_runs/AGENT-RUN-0011/metrics.json"

limitations:
  - "NMD-0002 contains exactly 2 odd-A nuclei, both even-Z/odd-N (O-17 and Fe-57)."
  - "There are zero odd-Z/even-N nuclei in the training set."
  - "c_oZeN would be completely unidentifiable; c_eZoN fits on 1-2 rows per holdout fold."
  - "Rejected as data-sparsity overfit before execution."

verdict: SANDBOX_PASS
maintainer_decision_requested: "Acknowledge rejection rationale."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "Rejected; no follow-up required."
