id: HYP-PROPOSAL-0042
title: "Rejected pairing-class plus shell-closure interaction"
campaign_profile_id: nuclear-mass-surface
proposal_kind: formula_search
status: REJECTED

claim_ceiling: "Rejected sandbox proposal; no experiment execution."

proposed_by:
  contributor_id: master
  agent_id: codex
  task_id: TASK-0201

input_references:
  - "docs/nuclear-mass-robustness-gate.md"
  - "hypothesis_proposals/nuclear-mass/HYP-PROPOSAL-0028-shell-gaussian-proximity-zn.yaml"
  - "hypothesis_proposals/nuclear-mass/HYP-PROPOSAL-0029-shell-gaussian-proximity-neutron-only.yaml"
  - "agent_runs/AGENT-RUN-0011/metrics.json"

hypothesis:
  statement: "Pairing-class indicators interacting with shell-closure flags might improve shell-adjacent residuals."
  variables:
    - "pairing_class_indicator: even-even, odd-A, or odd-odd class flag"
    - "shell_closure_indicator_k: one sparse indicator for a selected shell closure"
  assumptions:
    - "Shell-closure interactions would remain sparse on NMD-0002 and post-AME2020."
  expected_scope: "Rejected before execution."
  expected_failure_modes:
    - "Sparse region switches can overfit the small training surface."
    - "The family duplicates the shell-aware lane rather than testing a disjoint pairing lane."

novelty_check:
  checked_files:
    - "hypothesis_proposals/nuclear-mass/HYP-PROPOSAL-0028-shell-gaussian-proximity-zn.yaml"
    - "hypothesis_proposals/nuclear-mass/HYP-PROPOSAL-0029-shell-gaussian-proximity-neutron-only.yaml"
  possible_overlap: "Duplicates the shell-aware lane with additional pairing interactions."
  notes: "Rejected to preserve TASK-0201 as a disjoint pairing/odd-even lane."

falsification_plan:
  deterministic_check: "Pre-execution triage only; no sandbox execution selected."
  baseline_or_reference: "Frozen RESULT-0015 baseline."
  pass_condition: "Would require a separate shell-interaction task."
  fail_condition: "Rejected as duplicate-lane sparse region switching."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not mix sparse shell switches into the pairing lane after shell-aware batches are already visible."
  mitigations:
    - "Reject before sandbox execution."

sandbox_evidence:
  commands:
    - "python3 scripts/run_nuclear_pairing_batch.py"
  code_references:
    - "scripts/run_nuclear_pairing_batch.py"
  metrics:
    - "pre_execution_rejection_reason"
  output_paths:
    - "agent_runs/AGENT-RUN-0011/metrics.json"

limitations:
  - "Rejected during triage rather than executed."

verdict: REVIEW_NEEDED
maintainer_decision_requested: "Confirm rejection before execution to keep TASK-0201 disjoint from the shell-aware lane."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "No promotion path; shell-interaction work belongs in a separate maintainer-approved lane."
