id: HYP-PROPOSAL-0027
title: "Rejected live time-split fetch residual pilot"
campaign_profile_id: nuclear-mass-surface
proposal_kind: formula_search
status: REJECTED

claim_ceiling: "Rejected sandbox proposal; no experiment execution."

proposed_by:
  contributor_id: roman
  agent_id: codex
  task_id: TASK-0170

input_references:
  - "campaign_profiles/nuclear-mass-surface.yaml"
  - "docs/nuclear-mass-holdout-protocol.md"

hypothesis:
  statement: "A live fetch of later nuclear-mass measurements could support a time-split pilot eventually, but it is not allowed in the first pinned-slice sandbox task."
  variables:
    - "future post-AME2020 measurement batch"
  assumptions:
    - "Time-split holdout requires a separately committed reviewed dataset."
    - "The first pilot must not depend on live dataset fetching."
  expected_scope: "No execution scope; rejected during proposal triage."
  expected_failure_modes:
    - "Would violate the pinned-source and checksum policy."
    - "Would hide a dataset curation task inside a sandbox experiment."

novelty_check:
  checked_files:
    - "docs/nuclear-mass-holdout-protocol.md"
    - "campaign_profiles/nuclear-mass-surface.yaml"
  possible_overlap: "Time-split work is intentionally deferred until a later curated dataset task lands."
  notes: "Rejected to preserve pinned deterministic inputs."

falsification_plan:
  deterministic_check: "Source-policy triage only; no sandbox execution selected."
  baseline_or_reference: "Pinned NMD-0002 baseline dataset policy and the inactive time-split holdout rule."
  pass_condition: "Would require a separate reviewed dataset task before any pilot execution."
  fail_condition: "Rejected when the proposal depends on live or unpinned measurements."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not imply that later measurements were benchmarked without a committed dataset artifact."
  mitigations:
    - "Reject before sandbox execution and keep the time-split lane clearly deferred."

sandbox_evidence:
  commands:
    - "python3 -m physics_lab.cli preflight-research-proposal hypothesis_proposals/nuclear-mass/HYP-PROPOSAL-0027-live-time-split-fetch-rejected.yaml"
  code_references:
    - "physics_lab/registry/research_proposals.py"
  metrics:
    - "pre_execution_rejection_reason"
  output_paths:
    - "docs/reviews/autonomous-nuclear-mass-pilot-01.md"

limitations:
  - "Rejected during source-policy triage rather than executed."

verdict: REVIEW_NEEDED
maintainer_decision_requested: "Confirm rejection as a valid pinned-source boundary."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "Separate maintainer-reviewed dataset task before any time-split pilot."
