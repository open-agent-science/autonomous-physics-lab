id: HYP-PROPOSAL-0041
title: "Pairing exponent refinement eta(N,Z) / A^0.45 vs A^0.5"
campaign_profile_id: nuclear-mass-surface
proposal_kind: formula_search
status: REJECTED

claim_ceiling: "Rejected before execution; no sandbox evidence exists."

proposed_by:
  contributor_id: oleg
  agent_id: claude
  task_id: TASK-0201

input_references:
  - "campaign_profiles/nuclear-mass-surface.yaml"
  - "results/EXP-0012/RUN-0001/result.yaml"

hypothesis:
  statement: >
    Adding a correction r_corr = c * eta(N,Z) * (A^(-0.45) - A^(-0.5)) may
    capture the residual between the standard sqrt(A) pairing exponent and an
    empirically better-fitting exponent 0.45.
  variables:
    - "eta(N,Z): standard pairing indicator (+1 ee, 0 odd-A, -1 oo)"
    - "c: train-fitted coefficient in MeV"
  assumptions:
    - "The correction feature is not collinear with the baseline pairing term."
  expected_scope: "NMD-0002 structured holdout."
  expected_failure_modes:
    - "The correction feature is nearly collinear with eta(N,Z)/A^0.5 on the 11-row NMD-0002 slice."

novelty_check:
  checked_files:
    - "results/EXP-0012/RUN-0001/result.yaml"
  possible_overlap: "The feature eta(N,Z)*(A^{-0.45}-A^{-0.5}) is near-collinear with the baseline pairing feature."
  notes: "Rejected on near-collinearity grounds before execution."

falsification_plan:
  deterministic_check: "Not executed; rejected before execution."
  baseline_or_reference: "RESULT-0015 frozen baseline."
  pass_condition: "Not applicable; rejected."
  fail_condition: "Not applicable; rejected."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not claim any result from this unexecuted proposal."
  mitigations:
    - "Rejected before execution."

sandbox_evidence:
  commands:
    - "N/A: rejected before execution"
  code_references:
    - "scripts/run_nuclear_pairing_batch.py"
  metrics:
    - "N/A: rejected before execution"
  output_paths:
    - "agent_runs/AGENT-RUN-0011/metrics.json"

limitations:
  - "The correction feature A^(-0.45) - A^(-0.5) is nearly collinear with eta(N,Z)/A^0.5 on 11 rows."
  - "A rescaling of the baseline pairing coefficient achieves the same fit without a new parameter."
  - "Rejected on near-collinearity grounds before execution."

verdict: SANDBOX_PASS
maintainer_decision_requested: "Acknowledge rejection rationale."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "Rejected; no follow-up required."
