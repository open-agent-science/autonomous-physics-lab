id: HYP-PROPOSAL-0031
title: "Rejected N=82 targeted shell switch (post-hoc leakage)"
campaign_profile_id: nuclear-mass-surface
proposal_kind: formula_search
status: REJECTED

claim_ceiling: "Rejected sandbox proposal; no experiment execution."

proposed_by:
  contributor_id: roman
  agent_id: claude
  task_id: TASK-0200

input_references:
  - "docs/nuclear-mass-robustness-gate.md"
  - "agent_runs/AGENT-RUN-0008/metrics.json"
  - "hypothesis_proposals/nuclear-mass/HYP-PROPOSAL-0028-shell-gaussian-proximity-zn.yaml"

hypothesis:
  statement: "A feature targeted specifically at |N - 82| <= 2 might reduce the worst residuals on the post-AME2020 In and Sb cluster."
  variables:
    - "near_n82: 1 if |N - 82| <= 2, else 0"
  assumptions:
    - "Targeting a single shell closure would memorize a known failure region."
  expected_scope: "Rejected before execution."
  expected_failure_modes:
    - "Building a feature around an identified post-AME2020 failure cluster (In-128..134 and Sb-133 family) is retrospective leakage; the candidate would tune for known bad rows rather than test a general shell hypothesis."
    - "Such a feature would not generalize to other shell closures even within the same dataset."

novelty_check:
  checked_files:
    - "agent_runs/AGENT-RUN-0008/metrics.json"
    - "docs/nuclear-mass-robustness-gate.md"
  possible_overlap: "Same generic shell motivation as HYP-PROPOSAL-0028, but with a holdout-aware feature definition."
  notes: "Rejected explicitly under the gate's leakage-review requirement."

falsification_plan:
  deterministic_check: "Pre-execution triage only; no sandbox execution selected."
  baseline_or_reference: "Frozen RESULT-0015 baseline."
  pass_condition: "Would require an explicit maintainer-approved per-closure lane that registers features before the time-split surface is visible."
  fail_condition: "Rejected as post-hoc leakage from the AGENT-RUN-0008 worst-residual cluster."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not target individual shell closures based on retrospective time-split worst-residual lists."
  mitigations:
    - "Reject before sandbox execution."
    - "Document the leakage boundary."

sandbox_evidence:
  commands:
    - "python3 -m physics_lab.cli preflight-research-proposal hypothesis_proposals/nuclear-mass/HYP-PROPOSAL-0031-n82-targeted-rejected.yaml"
  code_references:
    - "physics_lab/registry/research_proposals.py"
  metrics:
    - "pre_execution_rejection_reason"
  output_paths:
    - "docs/reviews/autonomous-nuclear-shell-batch-01.md"

limitations:
  - "Rejected during triage rather than executed."

verdict: REVIEW_NEEDED
maintainer_decision_requested: "Confirm rejection under docs/nuclear-mass-robustness-gate.md leakage rules."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "No promotion path; per-closure features must be registered before any time-split surface is visible if such a lane is ever opened."
