id: HYP-PROPOSAL-0042
title: "Two-body valence pairing int(Z even) * int(N even) / A^2"
campaign_profile_id: nuclear-mass-surface
proposal_kind: formula_search
status: REJECTED

claim_ceiling: "Rejected before execution; no sandbox evidence exists."

proposed_by:
  contributor_id: oleg
  agent_id: claude
  task_id: TASK-0201

input_references:
  - "campaign_profiles/nuclear-mass-surface.yaml"
  - "hypothesis_proposals/nuclear-mass/HYP-PROPOSAL-0038-differential-pairing-ee-oo.yaml"

hypothesis:
  statement: >
    Adding a correction r_corr = c * int(Z%2==0) * int(N%2==0) / A^2 may
    capture a two-body valence proton-neutron pairing contribution to the
    residual beyond the baseline pairing term.
  variables:
    - "int(Z%2==0) * int(N%2==0): even-even class indicator"
    - "c: train-fitted coefficient in MeV"
  assumptions:
    - "The 1/A^2 scaling is distinct enough from the baseline pairing to provide independent information."
  expected_scope: "NMD-0002 structured holdout."
  expected_failure_modes:
    - "The feature int(Z%2==0) * int(N%2==0) is identical to eta_ee from HYP-PROPOSAL-0038; dividing by A^2 produces a near-redundant parameterization."

novelty_check:
  checked_files:
    - "hypothesis_proposals/nuclear-mass/HYP-PROPOSAL-0038-differential-pairing-ee-oo.yaml"
  possible_overlap: "int(Z%2==0) * int(N%2==0) is the same as eta_ee in HYP-PROPOSAL-0038."
  notes: "Rejected as redundant with HYP-PROPOSAL-0038 before execution."

falsification_plan:
  deterministic_check: "Not executed; rejected before execution."
  baseline_or_reference: "RESULT-0015 frozen baseline."
  pass_condition: "Not applicable; rejected."
  fail_condition: "Not applicable; rejected."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not claim any result from this unexecuted proposal."
  mitigations:
    - "Rejected before execution."

sandbox_evidence:
  commands:
    - "N/A: rejected before execution"
  code_references:
    - "scripts/run_nuclear_pairing_batch.py"
  metrics:
    - "N/A: rejected before execution"
  output_paths:
    - "agent_runs/AGENT-RUN-0011/metrics.json"

limitations:
  - "The even-even indicator is already captured by eta_ee in HYP-PROPOSAL-0038."
  - "Different A-scaling does not provide independent physical content on 11 training rows."
  - "Rejected as redundant parameterization before execution."

verdict: SANDBOX_PASS
maintainer_decision_requested: "Acknowledge rejection rationale."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "Rejected; no follow-up required."
