id: HYP-PROPOSAL-0017
title: "Rejected negative-lambda softening branch"
campaign_profile_id: anharmonic-oscillator
proposal_kind: formula_search
status: REJECTED

claim_ceiling: "Rejected sandbox proposal; no experiment execution."

proposed_by:
  contributor_id: roman
  agent_id: codex
  task_id: TASK-0160

input_references:
  - "campaign_profiles/anharmonic-oscillator.yaml"
  - "experiments/EXP-0011-anharmonic-oscillator-period.yaml"
  - "docs/results/anharmonic-oscillator-summary.md"

hypothesis:
  statement: "A softening branch with negative lambda should remain outside the current autonomous pilot because EXP-0011 only covers non-negative quartic coefficients."
  variables:
    - "lambda: quartic coefficient"
    - "A: oscillation amplitude"
  assumptions:
    - "The canonical benchmark explicitly excludes softening and double-well cases."
    - "Negative-lambda behavior needs a different deterministic scope and safety story."
  expected_scope: "No execution scope; rejected during proposal triage."
  expected_failure_modes:
    - "Mixes benchmark-scope expansion with autonomous-pilot packaging."
    - "Could blur a clean sandbox comparison into a new canonical benchmark request."

novelty_check:
  checked_files:
    - "experiments/EXP-0011-anharmonic-oscillator-period.yaml"
    - "docs/results/anharmonic-oscillator-summary.md"
  possible_overlap: "Outside current benchmark scope rather than overlapping an existing family."
  notes: "Rejected before execution because the current campaign profile is positive-lambda only."

falsification_plan:
  deterministic_check: "Pre-execution scope triage only; no sandbox run selected."
  baseline_or_reference: "Current EXP-0011 exact reference baseline, scope note, and campaign-profile boundary."
  pass_condition: "Would require a separate maintainer-approved benchmark-scope expansion."
  fail_condition: "Rejected while the campaign profile remains positive-lambda only."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not imply the current benchmark says anything about softening or double-well dynamics."
  mitigations:
    - "Frame rejection as a scope boundary, not a falsification claim."

sandbox_evidence:
  commands:
    - "python3 -m physics_lab.cli preflight-research-proposal hypothesis_proposals/anharmonic/HYP-PROPOSAL-0017-negative-lambda-softening-rejected.yaml"
  code_references:
    - "physics_lab/registry/research_proposals.py"
  metrics:
    - "pre_execution_rejection_reason"
  output_paths:
    - "docs/reviews/autonomous-anharmonic-pilot-01.md"

limitations:
  - "Rejected by scope triage rather than numerical execution."

verdict: INCONCLUSIVE
maintainer_decision_requested: "Confirm that any negative-lambda extension should be handled by a separate canonical benchmark task."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "A later maintainer-reviewed scope-expansion task if the campaign broadens."
