id: HYP-PROPOSAL-0016
title: "Rejected cubic epsilon extension"
campaign_profile_id: anharmonic-oscillator
proposal_kind: formula_search
status: REJECTED

claim_ceiling: "Rejected sandbox proposal; no experiment execution."

proposed_by:
  contributor_id: roman
  agent_id: codex
  task_id: TASK-0160

input_references:
  - "campaign_profiles/anharmonic-oscillator.yaml"
  - "results/EXP-0011/RUN-0001/result.yaml"

hypothesis:
  statement: "A cubic epsilon extension might lower train error further but is not suitable for the two-execution autonomous pilot budget."
  variables:
    - "epsilon: lambda*A^2/k"
    - "a, b, c: train-fitted coefficients"
  assumptions:
    - "The pilot should prefer one stronger family and one negative control, not several adjacent train-fit families."
    - "A cubic extension would be judged primarily by holdout discipline rather than train error."
  expected_scope: "No execution scope; rejected during proposal triage."
  expected_failure_modes:
    - "Consumes execution budget without teaching much more than the current empirical quadratic family."
    - "Raises overfitting risk before a maintainer chooses a canonical family-expansion task."

novelty_check:
  checked_files:
    - "results/EXP-0011/RUN-0001/result.yaml"
    - "experiments/EXP-0011-anharmonic-oscillator-period.yaml"
  possible_overlap: "High overlap with the current empirical quadratic benchmark family."
  notes: "Rejected before execution because the pilot budget favors broader contrast over one more train-fit extension."

falsification_plan:
  deterministic_check: "Pre-execution triage only; no sandbox run selected."
  baseline_or_reference: "Current RESULT-0014 exact reference baseline, benchmark family set, and two-execution pilot budget."
  pass_condition: "Would require a later canonical comparison task with a broader family-expansion scope."
  fail_condition: "Rejected while the pilot remains limited to two sandbox executions."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not imply that a lower train error alone justifies execution."
  mitigations:
    - "Frame rejection as pilot-budget and overlap triage, not as a truth judgment."

sandbox_evidence:
  commands:
    - "python3 -m physics_lab.cli preflight-research-proposal hypothesis_proposals/anharmonic/HYP-PROPOSAL-0016-cubic-epsilon-extension-rejected.yaml"
  code_references:
    - "physics_lab/registry/research_proposals.py"
  metrics:
    - "pre_execution_rejection_reason"
  output_paths:
    - "docs/reviews/autonomous-anharmonic-pilot-01.md"

limitations:
  - "Rejected by overlap and pilot-budget triage rather than execution."

verdict: INCONCLUSIVE
maintainer_decision_requested: "Confirm that nearby train-fit family extensions should wait for a separate canonical follow-up task."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "A later maintainer-reviewed family-expansion task if still interesting."
