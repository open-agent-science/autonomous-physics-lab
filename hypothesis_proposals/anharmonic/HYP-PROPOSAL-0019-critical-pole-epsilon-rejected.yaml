id: HYP-PROPOSAL-0019
title: "Rejected critical-pole epsilon candidate"
campaign_profile_id: anharmonic-oscillator
proposal_kind: formula_search
status: REJECTED

claim_ceiling: "Rejected sandbox proposal; no experiment execution."

proposed_by:
  contributor_id: roman
  agent_id: codex
  task_id: TASK-0160

input_references:
  - "campaign_profiles/anharmonic-oscillator.yaml"
  - "docs/drafts/anharmonic-period-experiment-plan.md"
  - "docs/autonomous-research-loop.md"

hypothesis:
  statement: "A singular candidate T/T0 = 1 / (1 - a*epsilon) should remain out of the first pilot because it imports unsupported pole language into a bounded anharmonic slice."
  variables:
    - "epsilon: lambda*A^2/k"
    - "a: train-fitted coefficient"
  assumptions:
    - "The current benchmark does not model a physical singularity inside the configured slices."
    - "Pole language should not be imported from other campaigns without explicit evidence."
  expected_scope: "No execution scope; rejected during proposal triage."
  expected_failure_modes:
    - "Encourages separatrix-like storytelling that the anharmonic benchmark does not need."
    - "Could look numerically good while adding unnecessary overclaim risk."

novelty_check:
  checked_files:
    - "docs/drafts/anharmonic-period-experiment-plan.md"
    - "docs/autonomous-research-loop.md"
  possible_overlap: "Cross-track narrative overlap with singularity-heavy approximation families."
  notes: "Rejected before execution because the first anharmonic pilot should stay away from pole framing."

falsification_plan:
  deterministic_check: "Pre-execution risk triage only; no sandbox run selected."
  baseline_or_reference: "Current bounded-slice exact reference baseline and the low-overclaim campaign profile."
  pass_condition: "Would require a later maintainer-reviewed task with explicit justification for singular candidates."
  fail_condition: "Rejected while the pilot remains scoped to low-overclaim bounded-slice families."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not imply a physical singularity from a sandbox fit."
  mitigations:
    - "Reject pole framing during pilot triage."

sandbox_evidence:
  commands:
    - "python3 -m physics_lab.cli preflight-research-proposal hypothesis_proposals/anharmonic/HYP-PROPOSAL-0019-critical-pole-epsilon-rejected.yaml"
  code_references:
    - "physics_lab/registry/research_proposals.py"
  metrics:
    - "pre_execution_rejection_reason"
  output_paths:
    - "docs/reviews/autonomous-anharmonic-pilot-01.md"

limitations:
  - "Rejected for overclaim-risk control rather than numerical execution."

verdict: INCONCLUSIVE
maintainer_decision_requested: "Confirm that singular-candidate families should remain outside the first anharmonic pilot."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "A separate maintainer-reviewed task before any singular-family exploration."
