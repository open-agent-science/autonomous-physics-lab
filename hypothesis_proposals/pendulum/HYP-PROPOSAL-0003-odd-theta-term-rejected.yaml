id: HYP-PROPOSAL-0003
title: "Rejected odd-theta pendulum correction"
campaign_profile_id: pendulum-formula-falsification
proposal_kind: formula_search
status: REJECTED

claim_ceiling: "Rejected sandbox proposal; no experiment execution."

proposed_by:
  contributor_id: roman
  agent_id: codex
  task_id: TASK-0153

input_references:
  - "campaign_profiles/pendulum-formula-falsification.yaml"
  - "docs/campaigns/pendulum-formula-falsification.md"

hypothesis:
  statement: "A correction containing a free theta term would reduce fit error through an unphysical odd component."
  variables:
    - "theta: pendulum amplitude in radians"
    - "a: fitted odd coefficient"
  assumptions:
    - "The ideal pendulum period ratio is even in theta."
    - "Candidate families should preserve evenness unless the campaign explicitly allows otherwise."
  expected_scope: "No execution scope; rejected during preflight reasoning."
  expected_failure_modes:
    - "Violates the expected even symmetry of the pendulum period ratio."

novelty_check:
  checked_files:
    - "docs/notes/pendulum-gauntlet-100.md"
    - "campaign_profiles/pendulum-formula-falsification.yaml"
  possible_overlap: "No accepted odd-theta family found."
  notes: "Rejected before sandbox execution because the family violates the profile's conservative candidate expectation."

falsification_plan:
  deterministic_check: "Preflight reasoning only; no numerical sandbox run selected."
  baseline_or_reference: "Exact reference baseline and evenness requirement from the campaign profile."
  pass_condition: "Would require a maintainer-approved reason to relax evenness."
  fail_condition: "Rejected when no such reason is present."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not use accidental fit improvement to weaken symmetry constraints."
  mitigations:
    - "Reject before execution and record the reason."

sandbox_evidence:
  commands:
    - "python3 -m physics_lab.cli preflight-research-proposal hypothesis_proposals/pendulum/HYP-PROPOSAL-0003-odd-theta-term-rejected.yaml"
  code_references:
    - "physics_lab/registry/research_proposals.py"
  metrics:
    - "preflight_rejection_reason"
  output_paths:
    - "docs/reviews/autonomous-pendulum-pilot-01.md"

limitations:
  - "Rejected by structural reasoning rather than numerical execution."

verdict: FALSIFIED
maintainer_decision_requested: "Confirm rejection as a valid pre-execution autonomy decision."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "None; retained only as rejected sandbox proposal memory."
