id: HYP-PROPOSAL-0008
title: "Distance plus velocity invalid classification"
campaign_profile_id: dimensional-analysis-validator
proposal_kind: classification
status: REVIEW_READY

claim_ceiling: "Sandbox challenge-item proposal only; no canonical result or claim promotion."

proposed_by:
  contributor_id: roman
  agent_id: codex
  task_id: TASK-0155

input_references:
  - "campaign_profiles/dimensional-analysis-validator.yaml"
  - "docs/autonomous-research-loop.md"
  - "docs/research-quality-gate.md"
  - "knowledge/challenge_sets/dimensional_analysis_challenge_set.yaml"

hypothesis:
  statement: "The expression x = d + v should classify as INVALID because it adds length and velocity."
  variables:
    - "x: length"
    - "d: length"
    - "v: length per time"
  assumptions:
    - "The validator rejects addition across incompatible SI dimensions."
    - "The item is intentionally simple as a negative-control candidate."
  expected_scope: "Sandbox classification of one invalid-units challenge-entry candidate."
  expected_failure_modes:
    - "The parser treats addition too permissively."
    - "The item is too trivial for later canonical curation."

novelty_check:
  checked_files:
    - "knowledge/challenge_sets/dimensional_analysis_challenge_set.yaml"
    - "docs/notes/dimensional-analysis-challenge-set.md"
  possible_overlap: "Similar in spirit to existing invalid-unit examples, but no exact x = d + v item found."
  notes: "Executed as a low-risk negative control for the second autonomy pilot."

falsification_plan:
  deterministic_check: "Run physics_lab.engines.dimensions.validate_item on x = d + v with length and velocity dimensions."
  baseline_or_reference: "Current challenge-set expected-verdict baseline plus deterministic SI-dimension validator."
  pass_condition: "Sandbox pass if observed verdict is INVALID and agrees with expected verdict."
  fail_condition: "Sandbox fail if observed verdict is VALID or INCONCLUSIVE."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not claim invalid examples are exhaustive."
    - "Do not claim the validator catches all dimensional mistakes."
  mitigations:
    - "Frame this as a negative-control sandbox item."
    - "Keep evidence out of canonical results."

sandbox_evidence:
  commands:
    - "python3 -m physics_lab.cli preflight-research-proposal hypothesis_proposals/dimensional-analysis/HYP-PROPOSAL-0008-distance-plus-velocity.yaml"
  code_references:
    - "physics_lab/engines/dimensions.py"
    - "physics_lab/registry/research_proposals.py"
  metrics:
    - "expected_verdict"
    - "observed_verdict"
    - "agrees"
  output_paths:
    - "agent_runs/AGENT-RUN-0003/metrics.json"

limitations:
  - "Sandbox-only negative-control example."
  - "Trivial mismatch may not be valuable enough for canonical curation."

verdict: SANDBOX_PASS
maintainer_decision_requested: "Decide whether this negative-control item is useful or too simple for future curation."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "Maintainer-reviewed canonical task before adding to accepted challenge memory."
