id: HYP-PROPOSAL-0012
title: "Rejected natural-unit energy-mass boundary"
campaign_profile_id: dimensional-analysis-validator
proposal_kind: classification
status: REJECTED

claim_ceiling: "Rejected sandbox proposal; no experiment execution."

proposed_by:
  contributor_id: roman
  agent_id: codex
  task_id: TASK-0155

input_references:
  - "campaign_profiles/dimensional-analysis-validator.yaml"
  - "docs/notes/dimensional-analysis-challenge-set.md"

hypothesis:
  statement: "The natural-unit relation E = m should remain out of the SI-focused validator pilot."
  variables:
    - "E: energy"
    - "m: mass"
    - "natural-unit convention c = 1"
  assumptions:
    - "The dimensional-analysis validator campaign is SI-focused unless a profile explicitly extends scope."
    - "Natural-unit examples need a separate convention-aware plan."
  expected_scope: "No execution scope; rejected during proposal triage."
  expected_failure_modes:
    - "Running the item under SI dimensions would confuse scope exclusion with physical falsity."

novelty_check:
  checked_files:
    - "docs/notes/dimensional-analysis-challenge-set.md"
    - "campaign_profiles/dimensional-analysis-validator.yaml"
  possible_overlap: "The challenge-set note excludes natural-unit and Planck-unit examples from v1 scope."
  notes: "Rejected before sandbox execution as an out-of-scope convention boundary."

falsification_plan:
  deterministic_check: "Pre-execution scope triage only; no validator run selected."
  baseline_or_reference: "SI-focused campaign-profile baseline and v1 scope note."
  pass_condition: "Would require a maintainer-approved natural-unit profile extension."
  fail_condition: "Rejected while the profile remains SI-focused."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not claim natural-unit formulas are invalid in general."
    - "Do not claim natural units are unsupported forever."
  mitigations:
    - "Frame rejection as a scope boundary, not a truth judgment."

sandbox_evidence:
  commands:
    - "python3 -m physics_lab.cli preflight-research-proposal hypothesis_proposals/dimensional-analysis/HYP-PROPOSAL-0012-natural-unit-energy-mass-rejected.yaml"
  code_references:
    - "physics_lab/registry/research_proposals.py"
  metrics:
    - "pre_execution_rejection_reason"
  output_paths:
    - "docs/reviews/autonomous-dimensional-validator-pilot-01.md"

limitations:
  - "Rejected by scope triage rather than dimensional execution."

verdict: INCONCLUSIVE
maintainer_decision_requested: "Confirm that natural-unit examples should remain outside this pilot."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "A separate maintainer-reviewed natural-unit scope task."
