id: HYP-PROPOSAL-0011
title: "Rejected Compton wavelength duplicate"
campaign_profile_id: dimensional-analysis-validator
proposal_kind: classification
status: REJECTED

claim_ceiling: "Rejected sandbox proposal; no experiment execution."

proposed_by:
  contributor_id: roman
  agent_id: codex
  task_id: TASK-0155

input_references:
  - "campaign_profiles/dimensional-analysis-validator.yaml"
  - "docs/notes/physical-constants-verification-track.md"

hypothesis:
  statement: "The relation lambda_C = h/(m*c) could classify as VALID, but it overlaps an existing physical-constants verification idea."
  variables:
    - "lambda_C: length"
    - "h: action"
    - "m: mass"
    - "c: speed"
  assumptions:
    - "Autonomous pilots should reject likely duplicates before sandbox execution."
    - "The physical constants track already names the Compton wavelength relation."
  expected_scope: "No execution scope; rejected during proposal triage."
  expected_failure_modes:
    - "Duplicate curation creates redundant review burden."

novelty_check:
  checked_files:
    - "docs/notes/physical-constants-verification-track.md"
    - "knowledge/challenge_sets/dimensional_analysis_challenge_set.yaml"
  possible_overlap: "CV-0008 already names Compton wavelength of electron as lambda_C = h/(m_e*c)."
  notes: "Rejected before sandbox execution as a duplicate-shaped candidate."

falsification_plan:
  deterministic_check: "Pre-execution novelty triage only; no validator run selected."
  baseline_or_reference: "Existing physical-constants verification track baseline."
  pass_condition: "Would require a maintainer decision that the duplicate is worth curation."
  fail_condition: "Rejected when duplicate risk is enough to avoid sandbox execution."

overclaim_risk:
  public_claim_allowed: false
  forbidden_language:
    - "Do not treat duplicate proposal generation as new evidence."
  mitigations:
    - "Reject before sandbox execution and record the overlap."

sandbox_evidence:
  commands:
    - "python3 -m physics_lab.cli preflight-research-proposal hypothesis_proposals/dimensional-analysis/HYP-PROPOSAL-0011-compton-wavelength-duplicate-rejected.yaml"
  code_references:
    - "physics_lab/registry/research_proposals.py"
  metrics:
    - "pre_execution_rejection_reason"
  output_paths:
    - "docs/reviews/autonomous-dimensional-validator-pilot-01.md"

limitations:
  - "Rejected by novelty triage rather than dimensional execution."

verdict: REVIEW_NEEDED
maintainer_decision_requested: "Confirm rejection as a valid duplicate-filtering step."

promotion_boundary:
  canonical_result_allowed: false
  claim_promotion_allowed: false
  required_next_step: "None unless a maintainer opens a separate curation task."
