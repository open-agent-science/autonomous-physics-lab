# Research Tasks

> This file is generated from canonical `tasks/TASK-*.yaml` files.
> Generated research-first navigation for executor agents and maintainers. Canonical storage remains `tasks/TASK-*.yaml`; this view hides DONE history so current science work is easier to scan.
> Refresh with `python3 -m physics_lab.cli sync-active-board .`.

## READY

- `TASK-0911` - Run a local-only canonical DZ10 full-table parity gate if AMDC bytes are available (`scientific_benchmark`, priority `medium`, difficulty `high`, domain `nuclear_physics`)
- `TASK-0920` - Run the Quantum ZnSe no-refit transfer benchmark only after contract approval (`scientific_benchmark`, priority `medium`, difficulty `medium`, domain `quantum_size_effects`)
- `TASK-0933` - Execute the approved tier-1 point-only NMD-0003 frontier prediction freeze (37 targets; mandatory caveat; no TASK-0827 unblock) (`scientific_benchmark`, priority `high`, difficulty `high`, domain `nuclear_physics`)

## IN_PROGRESS

None.

## REVIEW_READY

None.
