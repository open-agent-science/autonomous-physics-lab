# v0.2 Public-Alpha Go/No-Go Review — 2026-05

Task: `TASK-0312`
Branch: `agent/gladunrv/claude/task-0312-v02-public-alpha-go-no-go`
Base reviewed: `origin/main` at `1bd9ce3b19df1eede564399c7569b18bca4861f9`
Generated: 2026-05-21

## Verdict

`PUBLIC_AFTER_FIXES`

The repository's scientific and contributor-workflow evidence is now adequate
for a cautious `v0.2-public-alpha` opening, but a small set of release-time
docs and signoff alignments must clear before the public-opening switch is
flipped. None of the remaining blockers are missing scientific surfaces;
they are housekeeping items that keep public-facing docs internally consistent
with the current evidence state.

This artifact is review evidence for the maintainer. It does not make the
repository public and does not approve a release by itself.

## Scope

This review supersedes [v0.2-public-readiness.md](./v0.2-public-readiness.md)
(`PUBLIC AFTER NAMED BLOCKERS`, generated earlier in the `v0.2` cycle) by
checking the current state of:

- `docs/public-release-gates.md`
- `docs/reviews/public-release-validation-signoff.md`
- `docs/open-agent-network.md`
- `docs/status.md`
- `docs/mission-control.md`
- `docs/campaigns/nuclear-mass-surface.md`
- `README.md` campaign and Nuclear-box wording
- `tasks/ACTIVE.md` (READY / IN_PROGRESS / BLOCKED snapshot)
- `scripts/check_public_path_leaks.py` re-run on the current tree

It does not draft public-launch article or DOU content. It does not promote
any scientific claim. It does not modify result artifacts.

## Gate Recheck

### Gate 1 — Technical Stability

`PASS WITH MINOR ALIGNMENT`

Ready:

- canonical validation paths exist (`ruff`, `pytest`, `validate-repo --strict`,
  example runs, snapshot, review bundle);
- `scripts/check_public_path_leaks.py` reruns clean on the current task branch
  (`No public path leaks found.`);
- the release-time validation signoff artifact exists at
  [public-release-validation-signoff.md](./public-release-validation-signoff.md);
- `tasks/ACTIVE.md` and generated `docs/task-views/*.md` are in sync with the
  canonical YAML files.

Remaining alignment:

- the release-time validation signoff reviewed `origin/main` at
  `e06deac151bfa8056bee4300c35e4e81bf8170c3`. The current default-branch tip
  is `1bd9ce3b19df1eede564399c7569b18bca4861f9` (commits added since signoff
  include `TASK-0310`, `TASK-0313`, `TASK-0314`, `TASK-0315`, and the
  `TASK-0318` autonomous-runner closeout). A short signoff refresh against the
  actual public-opening cut commit is required before flipping public.

### Gate 2 — Multi-Agent / Open Agent Network Pilot

`PASS`

Evidence present:

- multi-agent dry-run, branch-based pilot evidence, and contributor metrics
  remain available and are linked from `docs/public-release-gates.md` and the
  private-contributor docs;
- [docs/open-agent-network.md](../open-agent-network.md) frames APL as a
  coordinated open agent network with shared campaigns, review gates, and no
  automatic claim promotion;
- nuclear scout, factory, and stress-test follow-ups demonstrate parallel-safe
  agent lanes through real merged PRs (`TASK-0285`, `TASK-0288`, `TASK-0289`,
  `TASK-0294`, `TASK-0295`, `TASK-0297`, `TASK-0310`, `TASK-0315`);
- maintainer review and closeout discipline is preserved (no agent-marked
  `DONE`, no auto-merge, no direct pushes to `main`).

### Gate 3 — Measurable Scientific Result

`PASS`

The flagship evidence surface is conservative and reproducible:

- `EXP-0001/RUN-0003` Pendulum Gauntlet 100 leaderboard, diagnostics, and
  precision audit;
- `EXP-0006/RUN-0006` Dimensional Analysis Validator MVP (49/50 agreement under
  explicit MVP limits);
- `EXP-0004`, `EXP-0005`, `EXP-0007`, `EXP-0008`, `EXP-0009` Koide/particle-mass
  reproductions and falsifications;
- `EXP-0011` Anharmonic Oscillator Period benchmark;
- `EXP-0012/RUN-0001` Nuclear Mass Baseline Residual Benchmark with sandbox
  pilot evidence and retrospective post-AME2020 time-split evidence kept
  `INCONCLUSIVE`;
- prospective nuclear prediction registry `PRED-0001`..`PRED-0068` remains
  framed as frozen forecasts, with `TASK-0305` `BLOCKED` until a reviewed
  source manifest and explicit maintainer approval exist;
- a visible negative-results registry preserves falsifications as
  first-class evidence rather than hiding them behind successful reproductions.

### Gate 4 — Public Narrative

`PARTIAL`

Ready:

- `README.md`, `docs/status.md`, `docs/mission-control.md`,
  `docs/public-release-gates.md`, and the external-reviewer replication guide
  preserve overclaim discipline;
- `EXP-0010` continues to be framed as a guarded stress-test surface, not a
  flagship public success;
- Nuclear Mass Surface wording in `docs/campaigns/nuclear-mass-surface.md`,
  `docs/status.md`, and `README.md` keeps `EXP-0012` framed as a baseline
  benchmark and prospective `PRED-*` entries framed as frozen forecasts;
- post-AME2020 evaluation is explicitly retrospective rather than strict blind
  prediction.

Remaining alignment:

- `docs/mission-control.md` line 15 still names the stage
  `v0.1-private-alpha — scientific campaign and contributor workflow validation`,
  while `docs/status.md` already names it
  `v0.2-public-alpha candidate — final release go/no-go review pending`. The
  mission-control stage line should match the status line before any public
  opening.
- The README's Nuclear-track box currently lists `EXP-0012` baseline,
  `AGENT-RUN-0005` sandbox, and the post-AME2020 guard. The current Nuclear
  evidence surface also includes the shell-axis prospective mini-wave
  (`PRED-0063`..`PRED-0068`), the `TASK-0310` full-known retrospective audit,
  and the `TASK-0315` validity-domain map. Either keep the README box narrow
  with one explicit "registry and shell-axis follow-up evidence is captured in
  the campaign page, not summarized here" sentence, or extend the box once with
  conservative language. Either choice should be made before the public cut so
  that the public-facing summary matches the campaign page.

## Open Agent Network Recheck

`PASS`

- coordination model in [docs/open-agent-network.md](../open-agent-network.md)
  remains aligned with the canonical task protocol;
- shared campaign surface ([nuclear-mass-surface.md](../campaigns/nuclear-mass-surface.md))
  preserves the "many agents can help test bounded nuclear-mass hypotheses;
  future source-pinned measurements may later decide which forecasts survive"
  framing, not a discovery claim;
- review gates, sandbox-first promotion, and no-auto-merge remain explicit;
- prospective `PRED-*` entries continue to read as frozen forecasts awaiting
  future maintainer-reviewed reveal data.

## Reveal Discipline Recheck

`PASS`

- `TASK-0303` (source preflight), `TASK-0304` (synthetic reveal dry-run on
  fabricated values only), and `TASK-0307` (source-manifest review) are merged
  and preserve the no-peek boundary;
- `TASK-0305` remains `BLOCKED` until a reviewed source manifest and explicit
  maintainer approval exist;
- the reveal protocol (`TASK-0266`) and the source readiness checklist remain
  the documented path for any future real reveal task;
- no public-facing doc describes prospective `PRED-*` entries as validated
  results.

## What Should Not Be Claimed (carried forward)

- APL does not provide final physics answers.
- APL has not produced discovery-level particle-mass explanations.
- Pendulum benchmark success is not a proof of exact symbolic validity.
- Koide-track reproductions and falsifications do not settle every Koide-like
  variant.
- `EXP-0010` is not a public success story.
- Nuclear Mass Surface evidence is benchmark, sandbox, and prospective-registry
  evidence, not a nuclear mass law or a discovery.
- Prospective `PRED-*` entries are frozen forecasts, not validated predictions.

## Remaining Blockers Before Public Opening

The following items must be cleared and re-checked before the public-opening
switch is flipped. Each is a small, concrete docs or signoff fix; none requires
new science.

1. **Realign mission-control stage wording with status.md.**
   Edit `docs/mission-control.md` line 15 so the named stage matches the
   `v0.2-public-alpha candidate` framing already in `docs/status.md`. Recommend
   capturing this as a small follow-up task
   (e.g. `realign mission-control stage wording before public opening`).
2. **Refresh the release-time validation signoff against the actual cut commit.**
   Update `docs/reviews/public-release-validation-signoff.md` (or add a dated
   addendum) so the "Base reviewed" commit matches whichever default-branch
   commit the maintainer intends to open publicly. The current artifact is
   pinned to `e06deac151bfa8056bee4300c35e4e81bf8170c3`; the current
   default-branch tip is `1bd9ce3b19df1eede564399c7569b18bca4861f9`.
3. **Decide README Nuclear-box wording for the cut.**
   Either (a) extend the README Nuclear-box once with conservative wording that
   names the shell-axis follow-up surface (`PRED-0063`..`PRED-0068`,
   `TASK-0310` full-known audit, `TASK-0315` validity-domain map) without any
   discovery framing, or (b) add one explicit sentence pointing readers to
   `docs/campaigns/nuclear-mass-surface.md` for the current Nuclear evidence
   surface. The chosen option should be a tiny docs PR, not a campaign rewrite.
4. **Re-run `scripts/check_public_path_leaks.py` and the strict
   validate-repo path on the cut commit at signoff time.**
   This re-run currently passes on the task branch; it should be repeated and
   recorded against the actual public-opening commit as part of the signoff
   refresh in (2).
5. **Confirm `TASK-0305` remains `BLOCKED` through the public opening.**
   Do not allow `TASK-0305` to move out of `BLOCKED` until the source manifest
   and no-peek conditions in `docs/nuclear-prediction-reveal-protocol.md` and
   `docs/nuclear-reveal-source-readiness-checklist.md` are satisfied and the
   maintainer has explicitly approved a real reveal.

The above items are docs-level alignment, not new science. Once they are
cleared and a fresh signoff exists against the cut commit, this review can be
re-issued as `PUBLIC_ALPHA_READY` without rerunning the whole gate review.

## Out Of Scope For This Task

- Do not draft public-launch article or DOU content.
- Do not promote any scientific claim or `PRED-*` entry to a result or
  knowledge status.
- Do not modify committed result artifacts under `results/` or `agent_runs/`.
- Do not change the `BLOCKED` status of `TASK-0305` from this review.
- Do not flip any docs to "public is open"; that is a separate maintainer
  decision after the signoff refresh.

## Release Decision

Do not open the repository publicly from the current state.

Open it only after the five blockers above are cleared on the same default-
branch commit that the maintainer intends to make public, the release-time
signoff is refreshed against that commit, and the maintainer explicitly accepts
this go/no-go artifact alongside the refreshed signoff.
