# v0.2 Public-Alpha Opening Signoff - 2026-06-28

Task: `TASK-0879`
Repository: `open-agent-science/autonomous-physics-lab`
Opening SHA: `8d91f7b2fe8a480326bcabbd3118960f493e549f`
Prior readiness cut: `a0ddb113aa5ac502a5396db14c9e5d7f89b9d075`
Generated: 2026-06-28

## Verdict

`PUBLIC_OPENING_CONFIRMED_AT_EXACT_SHA; GATE_5_CLOSED; GATE_6_RESCAN_CLEAN`

The repository was made public on 2026-06-28 after the
[readiness refresh](./v0.2-public-alpha-readiness-refresh-2026-06-28.md)
recorded all six public-opening gates as substantively green. This signoff
records the post-switch verification for the exact public-opening default-branch
SHA, confirms that `main` branch protection is enabled, and preserves the
source-artifact redistribution boundary.

This signoff does not create a release tag, mint a DOI, promote any
`RESULT-*`, `CLAIM-*`, `PRED-*`, or `KNOW-*` artifact, or change any scientific
verdict.

## Exact Opening SHA

`8d91f7b2fe8a480326bcabbd3118960f493e549f` is a generated task-navigation
commit:

```text
chore(board-sync): sync task navigation and safe closeouts after a0ddb113 [skip-board-sync]
```

It changed only `docs/task-views/release.md`. The last substantive merge before
opening was `0f1e757d3ae13b11211a6ed27efda8ac443917db` (`TASK-0879`, PR
#1283), whose default-branch CI completed successfully. No GitHub Actions run is
recorded for `8d91f7b2` itself because the commit was emitted by the board-sync
path with a skip marker; the exact-SHA checks below therefore record local
validation, public cloneability, branch protection, and history/source-artifact
guards at the opened SHA.

## Post-Public GitHub State

| Check | Result |
| --- | --- |
| Repository visibility | `PUBLIC` (`isPrivate: false`) |
| Default branch | `main` |
| Public clone probe | `git ls-remote https://github.com/open-agent-science/autonomous-physics-lab.git HEAD` returned `8d91f7b2fe8a480326bcabbd3118960f493e549f` without credentials |
| `main` branch protection | Enabled |
| Required checks | `Python fast tests (3.12)` and `Classify change set` |
| Strict required checks | Enabled |
| Enforce admins | Enabled |
| Force pushes | Disabled |
| Branch deletions | Disabled |
| Required conversation resolution | Enabled |
| Required approving reviews | `0` by maintainer choice; PR + required CI remain mandatory |

`Python tests (main matrix)` is intentionally not a required check because it is
the push-only matrix and is skipped on ordinary PRs.

## Exact-SHA Validation Log

All commands were run from a clean local checkout at `main =
8d91f7b2fe8a480326bcabbd3118960f493e549f`.

| Check | Command | Result |
| --- | --- | --- |
| Strict repo validation | `.venv/bin/python -m physics_lab.cli validate-repo . --strict --fail-on-warnings` | PASS (0 ERROR, 0 WARNING, 25 INFO) |
| Docs links | `.venv/bin/python -m pytest tests/test_docs_links.py` | PASS, 4 passed |
| Public path leak scanner | `.venv/bin/python scripts/check_public_path_leaks.py` | PASS, "No public path leaks found." |
| Public path leak test | `.venv/bin/python -m pytest tests/test_public_path_leaks.py` | PASS, 2 passed |
| Source-artifact redistribution guards | `.venv/bin/python -m pytest tests/test_source_artifact_publication_guard.py tests/test_data_redistribution_declarations.py` | PASS, 5 passed |
| Worktree cleanliness | `git status --short --branch` | clean, `main...origin/main` |

## Gate 5 Closeout

Gate 5 is closed for the public opening:

- repository visibility is public;
- `main` branch protection is enabled after the visibility switch;
- required checks are the two PR-universal checks (`Python fast tests (3.12)`,
  `Classify change set`);
- the push-only main matrix was not made required, avoiding a permanently
  skipped required check on PRs;
- `enforce_admins` is enabled, preserving the no-direct-push discipline.

The only deliberate compromise is `required_approving_review_count: 0`, because
many maintainer-agent PRs are authored by the same GitHub identity and GitHub
does not allow self-approval to satisfy a required approval. This should be
raised to `1` once an external reviewer or stable co-reviewer path is available.

## Gate 6 Closeout

The exact opened SHA passed the source-artifact/publication guards listed above.
No publisher PDFs, preprint PDFs, restricted third-party raw datasets, or
license-ambiguous source payloads were introduced by the opening commit. The
opening commit touched only generated task navigation.

## Remaining Release Decisions

The repository is public and branch protection is enabled. Still separate from
this opening signoff:

- release tagging;
- DOI minting;
- GitHub release notes;
- raising required approving reviews from `0` to `1` when a non-self-review path
  is ready.
