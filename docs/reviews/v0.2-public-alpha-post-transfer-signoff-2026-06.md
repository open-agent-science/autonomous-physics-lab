# v0.2 Public-Alpha Post-Transfer Signoff — 2026-06

> Current status note (2026-06-27 / `TASK-0858`): this document preserves the
> live GitHub observations from its reviewed cut. A later release-gate refresh
> reclassifies plan-limited private-repository branch protection as an immediate
> post-public hardening step, not a pre-public blocker, when the current GitHub
> plan only exposes branch protection after the repository becomes public. The
> same refresh also verifies that the old `TASK-0732` arXiv PDF history blocker
> is closed for current `origin/main`. See
> `docs/reviews/v0.2-public-alpha-release-gate-refresh-2026-06-27.md`.

Task: `TASK-0682`
Branch: `agent/master/codex/task-0682-post-transfer-release-signoff`
Base reviewed: `origin/main` at `f139d90d0fc6f776f130872ff3be4fc3c489d24a`
Generated: 2026-06-08

## Verdict

`PUBLIC_ALPHA_POST_TRANSFER_SIGNOFF_BLOCKED`

The local release-gate checks that were practical in this task environment
passed, and public-facing repository links now point at
`open-agent-science/autonomous-physics-lab`. The public-opening decision is
still blocked because the live GitHub metadata for the exact reviewed `main`
SHA does not show CI check-runs, and the `main` branch is reported as not
protected by the GitHub branch API.

This artifact does not make the repository public, create a release tag, mint a
DOI, score predictions, or promote scientific claims. It records post-transfer
evidence for maintainer review.

## Post-Transfer Live Metadata

| Surface | Observation | Signoff status |
| --- | --- | --- |
| Repository URL | `https://github.com/open-agent-science/autonomous-physics-lab` | PASS |
| Default branch | `main` | PASS |
| Live `main` SHA | `f139d90d0fc6f776f130872ff3be4fc3c489d24a` | PASS |
| Visibility | GitHub reports `PRIVATE` | Expected before maintainer public-opening decision |
| Branch protection | GitHub branch API reports `protected: false` | BLOCKER before public opening |
| Exact-SHA check-runs | `total_count: 0` for `f139d90d0fc6f776f130872ff3be4fc3c489d24a` | BLOCKER: no current CI evidence for this cut |
| Latest visible `CI` run | Older run `27097137706`, SHA `46bf2a4d64b7d19dac81645eb6b3966056d6b0b0`, conclusion `failure` | Not current-SHA evidence; do not use as pass |
| Runner API | `gh api repos/open-agent-science/autonomous-physics-lab/actions/runners` returned HTTP 404 | UNAVAILABLE from this agent context |
| Repository variables | `gh variable list` returned no visible variables | No runner-label variable evidence observed |
| Repository secrets | Not inspected | Intentionally not probed from agent environment |

## Link And Citation Metadata

| File | Observation | Status |
| --- | --- | --- |
| `README.md` | Clone URL uses `https://github.com/open-agent-science/autonomous-physics-lab.git` | PASS |
| `CITATION.cff` | `repository-code` and `url` use `https://github.com/open-agent-science/autonomous-physics-lab` | PASS |
| `.zenodo.json` | Software metadata is present and does not contain the old owner path | PASS |
| `docs/campaigns/public-science-dashboard.md` | Public campaign posture is current and non-claiming | PASS |
| Historical review docs | Older `gladunrv` URLs remain only in dated historical CI/review evidence | ACCEPTABLE HISTORICAL CONTEXT |

## Validation Log

Commands run from the `TASK-0682` worktree:

| Check | Command | Result |
| --- | --- | --- |
| Ruff | `python -m ruff check .` | PASS |
| Targeted docs / mission tests | `python -m pytest -o addopts='' tests/test_docs_links.py tests/test_mission_control.py` | PASS, 31 passed |
| Public path leak check | `python scripts/check_public_path_leaks.py` | PASS, `No public path leaks found.` |
| Core replay list | `python scripts/reproduce_core_results.py --list` | PASS; seven bounded core replay surfaces listed |
| Mission JSON | `python scripts/apl_mission.py --output json` | PASS |
| Strict repository validation | `python -m physics_lab.cli validate-repo . --strict --fail-on-warnings` | PASS, 0 errors, 0 warnings |

Strict validation emitted informational notes for draft claims with passing
evidence and duplicate proposal ids. These are existing INFO-level repository
states and are not release blockers under `--strict --fail-on-warnings`.

## Bounded Core Replay Scope

`scripts/reproduce_core_results.py --list` reports the current bounded replay
surface:

- `pendulum-gauntlet`: `RESULT-0004`, expected verdict `VALID_IN_RANGE`
- `dimensional-analysis`: `RESULT-0007`, expected verdict `VALID`
- `koide-charged-lepton`: `RESULT-0005`, expected verdict `VALID`
- `koide-tau-holdout`: `RESULT-0006`, expected verdict `VALID`, `VALID_IN_RANGE`
- `koide-neutrino-falsification`: `RESULT-0009`, expected verdict `INVALID`
- `koide-quark-falsification`: `RESULT-0010`, expected verdict `INVALID`
- `particle-mass-falsifier`: `RESULT-0011`, expected verdict `INVALID`

`EXP-0010` / Muon g-2 remains intentionally excluded from the default core
replay because it is a guarded empirical formula-search stress test, not a
public success story.

## Current Public Campaign Posture

The public dashboard currently frames the repository around reviewable
campaign surfaces rather than discovery claims:

- Exoplanet Mass-Radius is the most shareable public-safe benchmark surface,
  but the current null-baseline result is negative/control memory, not a
  planet-composition, habitability, or universal mass-radius-law claim.
- Nuclear Mass Surface remains the flagship validation and reveal-discipline
  campaign. Local-curvature evidence is falsification/negative memory, and
  reveal scoring remains blocked.
- Quantum Size Effects remains source-gated; Almeida and other direct-source
  work must pass checksum/source-artifact gates before scoring.
- Atomic-Clock Residuals remains covariance/source-readiness gated; Pizzocaro
  covariance work is support evidence, not constants-drift scoring.
- Textbook Formula Audit remains a scaffold and exact-reference/source-gate
  surface before empirical OOD claims.
- Materials Property Residuals has a provenance-rich `MD-0001` dataset and
  conservative baseline evidence, but no material recommendation or promoted
  materials-law result.

## Release Gate Recheck

### Gate 1 — Technical Stability

`BLOCKED`

Local technical checks passed, including lint, targeted tests, public path leak
check, mission JSON, core replay listing, and strict repository validation.
However, the exact live `main` SHA has no visible GitHub check-runs, and branch
protection is reported as disabled.

### Gate 2 — Multi-Agent / Open Agent Network Contributor Pilot

`PASS WITH OPERATIONAL FOLLOW-UP`

The repository continues to enforce task files, task branches, PR review,
claim-promotion limits, generated task views, and open-agent onboarding. Mission
JSON rendered successfully. Its READY science pool health notes are advisory:
11 science tasks across 6 surfaces, below the preferred queue depth but not a
validation failure.

### Gate 3 — Measurable Scientific Result And Negative Evidence

`PASS`

The bounded core replay list still includes successful benchmark surfaces and
negative/falsification surfaces. Current campaign pages preserve negative and
inconclusive evidence as first-class memory.

### Gate 4 — Public Narrative

`PASS`

`README.md`, citation metadata, and the public science dashboard use the
post-transfer repository path and keep public wording conservative. No new
discovery-level, final-answer, or universal-law claim was found in the checked
release surfaces.

### Gate 5 — Post-Transfer Repository Readiness

`BLOCKED`

Repository links and citation metadata are aligned with
`open-agent-science/autonomous-physics-lab`. The remaining blockers are
operational GitHub settings/evidence:

1. `main` branch protection is reported as disabled by the GitHub branch API.
2. The exact reviewed live `main` SHA has no visible check-runs.
3. Runner and secrets readiness could not be fully verified from this task
   environment; secrets were intentionally not probed.

## Required Maintainer Actions

Before changing repository visibility, the maintainer should:

1. Enable or confirm the intended `main` branch protection policy.
2. Produce a green CI run for the exact public-opening `main` SHA or record why
   that SHA intentionally has no check-runs.
3. Verify repository secrets and runner configuration from the maintainer admin
   UI or a trusted admin token.
4. Re-run this signoff or add a small dated addendum once those operational
   blockers are cleared.

## Explicit Non-Claims

- This signoff does not promote any `CLAIM-*`, `RESULT-*`, `PRED-*`, or
  `KNOW-*` artifact.
- This signoff does not score nuclear predictions or unblock nuclear reveal.
- This signoff does not turn Exoplanet diagnostics into planet-composition,
  habitability, target-priority, or universal mass-radius-law claims.
- This signoff does not start Quantum or Atomic benchmark interpretation before
  their source/covariance gates pass.
- This signoff does not create a release, mint a DOI, or make the repository
  public.

## Final Recommendation

Do not open the repository publicly from the exact reviewed state until the
post-transfer operational blockers are cleared. The documentation and local
repository validation surface are in good shape; the remaining release blocker
is GitHub-side readiness evidence for the exact public-opening cut.
