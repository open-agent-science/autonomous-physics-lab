# v0.2 Public-Alpha Readiness Refresh — 2026-06-28

Task: `TASK-0879`
Branch: `agent/gladunrv/claude/task-0879-public-release-readiness-refresh`
Base reviewed: `origin/main` at `a0ddb113aa5ac502a5396db14c9e5d7f89b9d075` (the 2026-06-28 audit cut)
Generated: 2026-06-28

## Verdict

`READINESS_REFRESH_AT_AUDIT_CUT; FINAL_EXACT_SHA_SIGNOFF_STILL_REQUIRED`

A full re-audit of all six public-release gates in
[../public-release-gates.md](../public-release-gates.md) finds them substantively
green at the 2026-06-28 audit cut. This refresh records that evidence and
forwards the two gate-doc status notes from the prior `TASK-0858` cut
(`15a9675b`, 2026-06-27) to the current `origin/main`, 55 intervening merged
commits later. It does not make the repository public, create a tag, mint a DOI,
promote any claim, or replace the final exact-SHA release signoff.

## Moving-HEAD Honesty Note

This refresh records the public-release readiness evidence **as observed at the
2026-06-28 audit on `origin/main = a0ddb113`**. It does **not** assert that
`a0ddb113` is the commit that will be opened: the PR delivering this refresh will
itself advance `main` past `a0ddb113`. The gate's exact-SHA control point is
**preserved, not discharged** — a final release signoff must re-run the
validation, public-path-leak, and Gate-6 history scans on whatever `main` SHA is
actually opened, and must observe green default-branch CI for that exact cut.
Treat this artifact as the all-gates-green readiness evidence as of 2026-06-28,
not as the final opening authorization.

## Relationship To Prior Signoffs

This refresh updates, and does not rewrite, the readiness picture last recorded
by [./v0.2-public-alpha-release-gate-refresh-2026-06-27.md](./v0.2-public-alpha-release-gate-refresh-2026-06-27.md)
(`TASK-0858` @ `15a9675b`) and the full gate-by-gate
[./v0.2-public-alpha-final-signoff-2026-05-31.md](./v0.2-public-alpha-final-signoff-2026-05-31.md)
(`TASK-0500`). The 2026-06-27 history-gate-cleared and branch-protection-
reclassified interpretations still hold and are re-confirmed here at the newer
cut. Those dated artifacts remain valid historical records for their own commits.

## Gate Recheck (audit cut `a0ddb113`, 2026-06-28)

### Gate 1 — Technical Stability: PASS

- `validate-repo . --strict --fail-on-warnings` -> PASS (0 ERROR, 0 WARNING, 25 INFO).
- `scripts/check_public_path_leaks.py` -> "No public path leaks found."
- `git ls-files .pytest_cache .ruff_cache` -> no output (0 tracked cache files).
- Branch cut cleanly from `origin/main`; no tracked OS or agent-local files.

### Gate 2 — Multi-Agent Contributor Pilot: PASS

- 1000+ merged PRs across four distinct contributors (`gladunrv`, `akutenyov`,
  `romanhladun24-dot`, `nazar-ivankiv`).
- Branch-based PRs link `TASK-*` ids; maintainer review precedes merge; no direct
  pushes to `main`; no automatic claim or knowledge promotion; review tiers remain
  labeled. The specific per-type PR quotas (sandbox / technical / replay) are a
  maintainer attestation from the PR list.

### Gate 3 — Measurable Result And Negative Evidence: PASS

- Bounded public result and benchmark surfaces (pendulum gauntlet, Koide
  reproduction, dimensional and nuclear baselines) and first-class falsification
  surfaces (the negative-results registry) remain visible with review-tier labels.
- No discovery-level or universal-law framing; the muon g-2 stress test stays
  cordoned off from the public success surface.

### Gate 4 — Public Narrative: PASS

- `README.md` carries a result summary; `docs/status.md` and the public dashboard
  describe campaign state without discovery-level claims.
- The repository description is accurate and non-overclaiming; announcement and
  release drafts exist and are verification-first; a citation path
  (`CITATION.cff`) exists with DOI minting deferred per policy; the external
  reviewer replication guide is present and scoped.

### Gate 5 — Post-Transfer Readiness: PASS at audit cut (maintainer actions pending)

- Contributor, release, and citation entrypoints point at
  `open-agent-science/autonomous-physics-lab`. Residual `gladunrv/...` references
  are historical pilot and CI evidence only — no live entrypoint points at the old
  org.
- Branch protection remains a maintainer post-public-switch action under the
  current plan, per the 2026-06-27 reclassification: record the intended `main`
  protection policy before opening, then enable it immediately after the
  visibility switch and before external invitations or broad announcement.

### Gate 6 — Public Artifact History And Redistribution Boundary: PASS at audit cut (exact-SHA re-scan pending)

- At `origin/main = a0ddb113`: 0 reachable `.pdf` blobs, 0 risky
  binary/document/archive additions, and the two `TASK-0732` arXiv PDF paths
  remain absent from default-branch history.
- This scan must be re-run on the exact commit that is opened (see the Moving-HEAD
  Honesty Note).

## Validation Log (run from the task branch at the audit cut)

| Check | Command | Result |
| --- | --- | --- |
| Strict repo validation | `python3 -m physics_lab.cli validate-repo . --strict --fail-on-warnings` | PASS (0 ERROR, 0 WARNING, 25 INFO) |
| Public path leaks | `python3 scripts/check_public_path_leaks.py` | PASS, "No public path leaks found." |
| Tracked cache files | `git ls-files .pytest_cache .ruff_cache` | PASS, no output |
| Reachable PDFs on default branch | `git rev-list --objects origin/main \| rg -i '\.pdf$'` | PASS, no output |
| Risky additions on default branch | `git log origin/main --diff-filter=A --name-only --pretty=format: \| rg -i '\.(pdf\|zip\|tar\|gz\|...)$' \| sort -u` | PASS, no output |
| Nemitz PDF path history | `git log --oneline origin/main -- data/atomic_clocks/source_artifacts/2016-nemitz-riken/arxiv-1601.04582.pdf` | PASS, no output |
| Beloy PDF path history | `git log --oneline origin/main -- data/atomic_clocks/source_artifacts/2021-beloy-bacon/arxiv-2005.14694.pdf` | PASS, no output |

`ruff` and `pytest` parity is covered by green default-branch CI (below) rather
than re-attested here, matching the audit's CI-on-main basis.

## GitHub CI

- Workflow `CI`, conclusion `success`, run
  https://github.com/open-agent-science/autonomous-physics-lab/actions/runs/28320814897
  for the latest substantive default-branch commit `120a2e65` (PR #1281).
- The audit cut `a0ddb113` is a `[skip-board-sync]` board-navigation commit on top
  of `120a2e65` and changes no code or scientific artifact.
- The final signoff must observe green CI for the exact opened SHA.

## Remaining Maintainer-Only Items

These stay open and are not discharged by this refresh:

1. **Final exact-SHA signoff** — re-run validation, public-path-leak, and Gate-6
   history scans, and observe green CI, on the exact `main` SHA that is opened.
2. **Branch protection after the public switch** — enable or confirm `main`
   protection immediately after the visibility flip, before external invitations
   or broad announcement (the current plan cannot enable it while private).
3. **The explicit visibility decision** — the maintainer must explicitly decide to
   flip visibility; this artifact does not decide it.

## Explicit Non-Claims

- No `RESULT-*`, `CLAIM-*`, `PRED-*`, or `KNOW-*` artifact is created, promoted, or
  modified.
- No source rows, benchmark metrics, or scientific verdicts are changed.
- This refresh does not authorize automatic merge, release tagging, DOI minting, or
  any visibility change.

## Recommendation

The repository's documented public-release gates are all substantively green at
the 2026-06-28 audit cut (`a0ddb113`). The remaining public-opening control points
are the final exact-SHA signoff and the two maintainer GitHub actions (the
visibility flip and post-switch branch protection). Keep the final exact-SHA
release signoff as the next control point.
