id: TASK-0740
title: "Run Stellar M-L Route 2 local-extractor benchmark"
type: scientific_benchmark
status: READY
closeout: review
difficulty: high
priority: high

strategy_alignment:
  - "Produce the first empirical Textbook Formula Audit benchmark only after Route 2 benchmark-readiness passes."
  - "Test the frozen mass-luminosity baseline on locally reproduced DEBCat rows without redistributing the full catalogue."
  - "Preserve range-aware, uncertainty-aware, and negative outcomes as scientific memory."

input:
  mode: workflow
  related_domain: textbook_formula_audit
  related_objects:
    - "tasks/TASK-0739-run-stellar-ml-benchmark-readiness-gate.yaml"
    - "tasks/TASK-0709-run-stellar-ml-first-empirical-audit.yaml"
    - "scripts/extract_debcat_stellar_ml_rows.py"
    - "data/textbook_formula_audit/stellar_ml/source_manifest.yaml"
  planning_context: >
    If TASK-0739 authorizes the local-extractor route, this task runs the first
    empirical Stellar M-L audit using locally reproduced DEBCat rows, frozen
    system-level holdout lanes, and the predeclared alpha=3.5 baseline.

requirements:
  - "Start only after TASK-0739 explicitly authorizes the Route 2 local-extractor benchmark path."
  - "Fetch/verify DEBCat only through the approved local artifact path; do not commit raw debs.dat or the full normalized row set."
  - "Use the frozen physical-binary-system holdout lanes before any residual inspection."
  - "Report per-slice metrics, uncertainty handling, derived-luminosity sensitivity, and limitations."
  - "Do not claim a universal stellar law, stellar-evolution result, or discovery."

accepted_outputs:
  - "agent_runs/AGENT-RUN-XXXX/agent_run.yaml"
  - "agent_runs/AGENT-RUN-XXXX/metrics.json"
  - "agent_runs/AGENT-RUN-XXXX/report.md"
  - "docs/reviews/stellar-ml-route2-local-benchmark.md"
  - "optional RESULT candidate only if Gate A passes and maintainer approval allows publication"

validation:
  commands:
    - "python3 -m ruff check ."
    - "python3 -m pytest tests/test_stellar_ml_debcat_rows.py"
    - "python3 -m physics_lab.cli validate-repo . --strict --fail-on-warnings"

can_be_done_by:
  - human
  - codex
  - claude_code
  - llm_agent
