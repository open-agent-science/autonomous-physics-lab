id: TASK-0649
title: "Package Exoplanet negative/control result-publication preflight"
type: scientific_result_publication
status: DONE
difficulty: high
priority: high

strategy_alignment:
  - "Turn Exoplanet's strongest current value into a reviewable negative/control result candidate."
  - "Avoid another residual pilot on the same pinned snapshot until the source gate changes."
  - "Make the public story about where simple mass-radius baselines fail under controls."

input:
  mode: workflow
  related_domain: exoplanet_mass_radius
  related_objects:
    - "docs/reviews/exoplanet-mass-radius-residual-failure-map.md"
    - "docs/reviews/exoplanet-null-baseline-family-audit.md"
    - "docs/reviews/exoplanet-second-snapshot-negative-memory-routing.md"
    - "docs/result-promotion-protocol.md"
    - "results/RESULT-TEMPLATE.agent-published.yaml"
  planning_context: >
    Exoplanet follow-up lanes repeatedly preserved control-sensitive negative
    memory. This task decides whether that can be packaged as a scoped
    negative/control RESULT candidate without planet-law or habitability claims.

requirements:
  - "Check Gate A readiness for a scoped Exoplanet negative/control result artifact."
  - "If Gate A passes, create exactly one AGENT_PUBLISHED RESULT candidate about control-sensitive residual failure, not a planet law."
  - "If Gate A is blocked, preserve the blocker review and do not create a RESULT artifact."
  - "Do not fetch new rows, run new residual metrics, or make habitability/composition/target-priority claims."

accepted_outputs:
  - "docs/reviews/exoplanet-negative-control-result-publication-preflight.md"
  - "optional results/EXP-0014/RUN-0002/result.yaml only if Gate A passes and experiment identity exists"
  - "tests only if a result-packaging helper or fixture is added"

validation:
  commands:
    - "python3 -m ruff check ."
    - "python3 -m pytest tests/test_result_publication_gate.py tests/test_docs_links.py"
    - "python3 -m physics_lab.cli validate-repo . --strict --fail-on-warnings"

can_be_done_by:
  - human
  - codex
  - claude_code
  - llm_agent
