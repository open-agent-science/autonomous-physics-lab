id: TASK-0919
title: "Package Exoplanet null-baseline negative/control memory as a RESULT after identities exist"
type: scientific_result_publication
status: DONE
closeout: review
difficulty: medium
priority: medium

strategy_alignment:
  - "The null-baseline negative/control memory is replayable and canonical identities now exist via TASK-0909."
  - "Packaging should happen only after identity scaffolding, and only without reopening residual scoring."
  - "This scoped task preserves the next step so agents do not invent a new residual lane."

input:
  mode: workflow
  related_domain: astrophysics
  related_objects:
    - "docs/reviews/exoplanet-null-baseline-canonical-identity-decision.md"
    - "docs/reviews/exoplanet-null-baseline-negative-memory-replay.md"
    - "docs/reviews/exoplanet-null-baseline-family-audit.md"
    - "docs/reviews/exoplanet-negative-control-gate-a-blocker.md"
    - "docs/result-promotion-protocol.md"
  planning_context: >
    TASK-0909 created the required canonical identities. Package the already
    replayed null-baseline control-sensitive finding as a scoped
    negative/control RESULT candidate if Gate A passes.

requirements:
  - "Proceed only because TASK-0909 is DONE; do not start without the committed canonical identities."
  - "Use only committed EXO-0001 evidence and the identities created by TASK-0909."
  - "Package a deterministic result only if command, inputs, hashes, verification, limitations, and no-claim wording satisfy Gate A."
  - "Do not fetch live data, create EXO-0003 rows, change trigger thresholds, rerun residual scoring, or create a positive mass-radius law."
  - "Stop if canonical identity or deterministic command support is still incomplete."
  - "Update hypotheses/HYP-0021-exoplanet-null-baseline-control-sensitivity.yaml only to reference RESULT-0027 as evidence; do not change its status or scientific wording."

accepted_outputs:
  - "A Gate A PASS/BLOCKED negative/control RESULT package or a blocker note."
  - "A canonical result artifact package under results/EXP-0021/RUN-0001/."
  - "Output-routing summary with Gate A status, Gate B next step, claim impact none, and monitor-only posture preserved."

result_artifact_policy:
  required: true
  reason: "This task exists only to package an already replayed negative/control result after identity gates clear."
  evidence:
    - "docs/reviews/exoplanet-null-baseline-negative-memory-replay.md"
    - "results/EXP-0021/RUN-0001/result.yaml"
    - "results/EXP-0021/RUN-0001/gate_a_report.md"

validation:
  commands:
    - "python3 -m ruff check ."
    - "python3 -m pytest -n 0 -p no:cacheprovider --basetemp=.pytest-basetemp-task0919 tests/test_docs_links.py tests/test_exoplanet_mass_radius_loader.py tests/test_exoplanet_null_baseline_result_package.py"
    - "python3 scripts/apl_check_result_publication.py results/EXP-0021/RUN-0001/result.yaml"
    - "python3 -m physics_lab.cli validate-repo . --strict --fail-on-warnings"

can_be_done_by:
  - human
  - codex
  - claude_code
  - llm_agent
