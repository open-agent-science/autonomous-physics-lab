id: TASK-0759
title: "Run Stellar M-L stage-control and split-sensitivity audit"
type: scientific_validation
status: DONE
closeout: review
difficulty: high
priority: high

strategy_alignment:
  - "Advance the Textbook Formula Audit only through controls that can decide Gate A readiness."
  - "Separate a potentially real stage-specific mass-luminosity signal from split luck, leakage, and underpowered baselines."
  - "Avoid creating a RESULT artifact until the current sandbox-pass evidence survives predeclared controls."

input:
  mode: planning_only
  related_domain: textbook_formula_audit
  related_objects:
    - "docs/reviews/stellar-ml-route2-local-benchmark.md"
    - "docs/reviews/stellar-ml-route2-promotion-readiness-scorecard.md"
    - "agent_runs/AGENT-RUN-0070/metrics.json"
    - "scripts/run_stellar_ml_route2_local_benchmark.py"
    - "scripts/extract_debcat_stellar_ml_rows.py"
    - "data/textbook_formula_audit/stellar_ml/source_manifest.yaml"
    - "docs/result-promotion-protocol.md"
  planning_context: >
    TASK-0740 produced a Stellar M-L Route 2 local benchmark with a sandbox-pass
    outcome, and TASK-0753 judged it NOT_YET_GATE_A_READY. The next scientific
    step is a controlled audit: stage restriction, deterministic null/shuffle
    controls, split sensitivity, and baseline adequacy before any RESULT
    packaging.

requirements:
  - "Use only the committed extraction/benchmark scripts and the approved Route 2 local-source path; do not fetch new value-bearing rows in this task."
  - "If the local DEBCat data needed for the audit is unavailable, stop with an explicit source-readiness blocker instead of fabricating rows or substituting another source."
  - "Predeclare the stage policy before reading metrics: primary main-sequence-compatible lane, with evolved/unknown rows treated only as diagnostics unless the task explains a stricter exclusion."
  - "Run deterministic null/shuffle controls and seeded split-sensitivity checks against the same frozen input."
  - "Compare the textbook baseline against at least one simple single-alpha baseline and state whether a future piecewise/stage-aware baseline is required before Gate A."
  - "Do not create a RESULT/PRED/CLAIM/KNOW artifact, promote textbook-law claims, or publish raw third-party DEBCat content beyond the existing source policy."

accepted_outputs:
  - "docs/reviews/stellar-ml-route2-stage-control-split-audit.md"
  - "scripts/run_stellar_ml_stage_control_audit.py"
  - "agent_runs/AGENT-RUN-0073/metrics.json"
  - "agent_runs/AGENT-RUN-0073/report.md"
  - "explicit recommendation: baseline-adequacy follow-up then maintainer-gated Gate A (controls passed)"

result_artifact_policy:
  required: false
  reason: "Control and readiness audit before any RESULT packaging; no scientific RESULT or PRED artifact is expected."
  evidence:
    - "docs/reviews/stellar-ml-route2-stage-control-split-audit.md"

validation:
  commands:
    - "python3 -m ruff check ."
    - "python3 -m pytest tests/test_docs_links.py tests/test_textbook_stefan_boltzmann_exact_reference.py"
    - "python3 -m physics_lab.cli validate-repo . --strict --fail-on-warnings"

can_be_done_by:
  - human
  - codex
  - claude_code
  - llm_agent
