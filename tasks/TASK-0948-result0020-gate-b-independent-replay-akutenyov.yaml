id: TASK-0948
title: "Run the RESULT-0020 Gate B replay with a genuinely independent identity (reserved: akutenyov, non-Claude tool)"
type: scientific_replay_validation
status: READY
closeout: review
difficulty: low
priority: high

strategy_alignment:
  - "Decision Day #2 R4 (2026-07-06): RESULT-0020 has been AGENT_PUBLISHED for 21+ days across four tasks because every attempted replayer failed the human-level independence standard; akutenyov is the only identity that clears it."
  - "TASK-0941 recorded the routing lesson: an unreserved validation task gets burned by an ineligible lane; this successor is identity-reserved at seed time."
  - "A PASS applies the first validation_record under the R1 independence axis with validation_independence: independent, and unblocks the cheapest claim refresh (CLAIM-0005)."

input:
  mode: workflow
  related_domain: dimensional_analysis_validator
  related_objects:
    - "docs/reviews/maintainer-decision-day-2026-07-06.md"
    - "docs/reviews/dimensional-result-0020-gate-b-independent-replay.md"
    - "docs/reviews/dimensional-result-0020-gate-b-validation-independence-blocker.md"
    - "results/EXP-0006/RUN-0007/result.yaml"
  planning_context: >
    The mechanical replay is known-good (TASK-0916 recorded a zero-drift PASS
    over 17 metrics at tol 1e-9) and the packaging is Gate-B-safe since
    TASK-0782. The only missing ingredient is an eligible identity. This
    task re-runs the documented Gate B command as akutenyov with a
    non-Claude tool and, on PASS, applies the metadata-only tier update.

requirements:
  - "Reserved for contributor akutenyov using a non-Claude tool (publisher and prior fix author are gladunrv/Claude); any other identity must stop with INDEPENDENCE_BLOCKED rather than replay."
  - "Record replayer contributor_id, github_username, agent_tool, and model before running; replay the documented Gate B command from committed inputs only."
  - "On zero-drift PASS: update only review_tier and validation_record metadata, including validation_independence: independent per the R1 axis; change no metrics, challenge set, verdicts, CLAIM-0005 content, or knowledge artifacts."
  - "On failure or drift: write a blocker note and leave RESULT-0020 AGENT_PUBLISHED."
  - "No wording that treats dimensional validity as evidence of physical correctness or supports a broader scientific claim."

accepted_outputs:
  - "The replay note with recorded identity and command transcript, plus the metadata-only tier update on PASS."
  - "Output-routing summary: Gate B metadata validation only; no scientific content change."

result_artifact_policy:
  required: false
  reason: "Metadata-only Gate B validation of an existing result; no new scientific result artifact is produced."
  evidence:
    - "results/EXP-0006/RUN-0007/result.yaml"

validation:
  commands:
    - "python3 -m pytest tests/test_docs_links.py"
    - "python3 -m physics_lab.cli validate-repo . --strict --fail-on-warnings"

can_be_done_by:
  - human
  - codex
