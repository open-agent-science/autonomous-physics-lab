# Limitations — AGENT-RUN-0001

- Sandbox-only evidence; no canonical `RESULT-*` artifact was created.
- The candidate does not beat the current `t4_x1` reference candidate on the
  configured test range.
- Stress-range behavior is materially worse than the configured test-range
  behavior.
- The rational denominator was bounded during fitting; broader pole searches
  need a separate reviewed task.
- No claim, knowledge, hypothesis, or experiment object is promoted.
