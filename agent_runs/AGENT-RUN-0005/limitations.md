# Limitations - AGENT-RUN-0005

- This is sandbox-only evidence and does not update `RESULT-0015`.
- The pilot uses a tiny measured-only slice, so even the strongest executed family is not broad scientific evidence.
- Structured holdout is informative here, but a later canonical comparison task still needs a broader pinned dataset.
