# Limitations - AGENT-RUN-0006

- This is a sandbox replay, not a canonical `RESULT-*` update.
- The replay uses the tiny pinned `NMD-0002` measured-only slice; it is not an
  external validation set.
- `HYP-PROPOSAL-0021` was selected after active holdout performance was visible
  in `AGENT-RUN-0005`, so the replay does not remove selection leakage risk.
- The same-shape split enumeration is useful for stress testing, but all splits
  still come from the same small committed surface.
- The odd-A term shows narrow partial value; it does not justify a broad
  nuclear-mass claim or a universal correction statement.
- Any second nuclear sandbox batch should preserve this replay as visible
  negative/partial evidence beside new candidates.
