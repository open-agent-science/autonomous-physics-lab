{
  "_profile": "safe",
  "_description": "Empty local override. Relies entirely on the committed baseline allow list in .claude/settings.json. Any command outside the baseline triggers a manual approval prompt. Recommended for onboarding and unfamiliar work.",
  "permissions": {
    "allow": []
  }
}
